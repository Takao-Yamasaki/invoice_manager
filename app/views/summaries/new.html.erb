<p>請求内容の登録</p>
<%= form_with model: @summary , local: true do |f| %>
  <p>請求ID：<%= f.text_field :invoice_id %></p>
  <p>顧客名：<%= f.collection_select :client_id,Client.all,:id,:name,include_blank: "顧客名を選択してください" %></>
  <p>商品名：<%= f.collection_select :item_id,Item.all,:id,:name,include_blank: "商品名を選択してください" %></p>
  <p>数量：<%= f.text_field :amount %></p>
  <p>登録日：<%= f.text_field :registration %></p>
  <%= f.submit "登録"%>
<% end %>